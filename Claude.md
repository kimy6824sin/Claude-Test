# 逆向工程建模软件 (ReverseCAD Engine)

## 项目定位
类似于 Geomagic Design X 的轻量化逆向工程建模工具，专门针对活塞发动机机械零件（活塞、连杆、曲轴、气缸套等）的点云/网格（Mesh）模型进行特征提取与 B-Rep (CAD) 实体建模。

## 推荐技术栈 (Python 快速原型)
- **GUI 交互**: PyQt6 / PySide6
- **3D 视口与网格渲染**: PyVista / VTK
- **网格算法与几何拟合**: Open3D, Trimesh, Scipy, NumPy
- **CAD 实体内核与 STEP 导出**: pythonocc-core (Open CASCADE Technology) 或 build123d / CadQuery
- **偏差分析**: Scipy KDTree, PyVista 点云距离色谱图

## 核心业务流水线
1. 网格导入与预处理：STL/OBJ/PLY 导入、去噪、坐标系对齐、曲率分割。
2. 特征基元提取：RANSAC 算法提取平面、圆柱体（孔/轴）、圆锥、球体。
3. 截面提取 (Mesh Sketching)：平面切片网格生成 2D 轮廓散点，拟合直线、圆弧、样条线。
4. 特征建模：基于拟合草图执行拉伸、旋转、布尔运算生成 B-Rep 实体，导出 STEP 格式。
5. 偏差检验 (Accuracy Analyzer)：CAD 面到原始 Mesh 的距离热力图分析（公差带控制）。

## 代码规范与要求
- 模块化设计，业务逻辑与 UI 渲染严格分离。
- 所有数学拟合函数需包含清晰的数学原理注释与输入输出类型标注 (Type Hints)。
- 关键算法编写单元测试（使用 pytest）。

## 其它说明
1.像这种工业零部件都是由一些基本的形状（圆、球、圆柱、长方体、正方形等）经过拉伸、挖槽、阵列等操作完成的，你需要了解建模的基本流程和规范；
2.你需要具备机械设计的原理、流程等知识，例如齿轮的啮合面肯定是光滑的，不能出现歪歪扭扭的情况；
3.你需要具备识别零件部件意图的能力，例如有些孔是为了安装螺栓、有些筋条是为了加强等；
4.对于接触面、安装面等精度要求更高，其它地方的精度要求可以低一些；
5.当你识别到了参数化模型，要勇于剔除一些噪声，不要被其干扰；
